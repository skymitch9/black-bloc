# Owner guide — before the trip, on the road, when you're back

> **Audience:** you (the owner), on your phone or the laptop. **Status:** TRACKED (owner, 2026-08-31 —
> permanently, not temporarily; secret NAMES only).
> Last verified: **2026-09-11 08:39** — the docs-wide staleness pass checked every count and every
> link on this page. Corrections made: the *Type a link into an event's Where* row is ✅ **LIVE — v108,
> merge `73e2e44`, deployed 2026-09-11 00:37** (this header said "NOT merged, NOT deployed" — it was
> written 16 minutes before the merge); the **sweeps count is 350**, not 261; and §5's "decide whether
> to keep `docs/` tracked" was decided long ago (**keep**, owner 2026-08-31). Measured today: **29**
> top-level slash commands with **zero** groups left, matching the "30 → 29 with ZERO groups" line in
> the `/settings` row; **19** cogs; **17** dashboard pages, so every `https://blackbloc.heygabi.ai/*.html`
> link on this page points at a file that exists; **18** panels, each with its own
> `<feature>_panel_minutes` key defaulting to **10**, which is what the "(10)" in a dozen rows below
> means; `selftest_purge_minutes` really is **1**. ⚠️ **NOT checked:** ⚠️ **nothing on this page has been
> run against Discord or in a browser by this pass** — no slash command was typed, no button pressed,
> no page opened. Every row is still "what it should do", verified from the code and the settings
> registry only. Before that, **2026-09-11 00:21** for the WHERE FOLLOW-UP 4 row (written on branch
> `where-smart`, since merged). Before that,
> **2026-09-05** for the sweeps count (**261** numbered rows at the time — 252–261 are the
> SELF-TEST build's (wave 5), written as `ST1`–`ST10` and numbered at the merge, shipped as **v86**; 245–251 are the
> MODMAIL follow-up's (the ticket card ON the panel), written as `ML1`–`ML7` and numbered at the
> merge, shipped as **v85**; 231–244 the SETTINGS PANEL's two builds (v83/v84); 219–230 are the
> MODMAIL PANEL build's **Build B**, written as `MB1`–`MB12` on `worktree-agent-a4bebd98196e3ca14`
> and numbered at the merge, shipped as **v82**; 208–218 are the
> MOD CASES PANEL build's, written as `C1`–`C11` on its branch and numbered at the merge; 200–207 the
> MODMAIL PANEL build's (Build A), written as `MA1`–`MA8` on its branch and numbered at the merge; 188–199 the
> HONEYPOT PANEL build's, written as `H1`–`H12` on its branch and numbered at the merge; 183–187 the
> HIDE COMMANDS WHEN OFF build's; before them, 173–182 are the
> ROLE MENUS PANEL build's, written as `M1`–`M10` on its branch and numbered at the merge; 163–172 the
> RAID-TRAIN PANEL build's; 155–162 the
> CHAT PANEL build's, written as `C1`–`C8` on its branch and numbered at the merge; 144–154 the
> AUTOMOD PANEL build's; 135–143 the
> TEMP-VOICE PANEL build's; 126–134 the PINGS
> PANEL build's; 118–125 the YOUTUBE
> PANEL build's, written as 130–137 on its branch and renumbered at the merge; 109–117 the go-live panel's,
> 104–108 the MEMORY PANEL build's, and 103 the operator token's; counted against [`sweeps.md`](sweeps.md) itself)
> and the GO-LIVE row (`/golive` is ONE command that opens a panel; the `golive` and `twitch` groups are gone;
> not run against Discord by eye).
> ⚠️ This guide names no `/uploads` subcommand anywhere — measured 2026-09-03, it names no
> YouTube command at all, so the panel changed nothing here.
> ⚠️ This guide names `/memory` nowhere and still does not — the panel is member-only and
> owns no owner-facing operation. Same day, for the APPLICATIONS row (`/apply` is ONE command that opens a
> panel; both the `apply` and `applications` groups and their seventeen subcommands are gone;
> live in v66 since 15:00; not run against Discord by eye) and the sweeps
> count (**95** rows, numbered to 102). Same day, the POLLS row (`/poll` is one command that opens
> a panel — live in v65 14:14; not run by eye), the EVENTS row (`/event` is one command that opens a panel and
> `/timezone` is gone — live in v64 since 14:05; not run against Discord by eye) and
> the birthdays row (`/birthday` is ONE command that opens a panel — live in v63). Before that the same day, for the applications row
> (a form may keep a LIST instead of handing a
> role over — live in v62 since 12:48) and the sweeps count
> (69 rows, numbered to 72; 66–68 belong to the sibling requests-check build). Before that the same
> day, for the requests row only — the `/request` panel no longer writes a
> member's own requests out (`request_panel_own_list`, off by default), and the sweeps count was
> re-counted against [`sweeps.md`](sweeps.md) (**68**, was 65 this morning and 42 before that; rows
> 66–68 are the sixth pass, built on `feat/requests-check` and not yet merged).
> ⚠️ Not run against live Discord. Before that,
> **2026-08-27 20:22** — commands were run from the main machine that week; the laptop steps
> are the same tools on a fresh clone and have NOT been rehearsed on your laptop.
> Details live in [`runbook.md`](runbook.md) (operations) and [`sweeps.md`](sweeps.md) (what to test); this
> page is the order to do things in.

## 1. Before you leave (main machine, ~5 minutes)

1. **Encrypt `.env` so the laptop can have it** — in a **Git Bash** terminal (VS Code: Terminal ▸ the ▾ next
   to `+` ▸ Git Bash; or right-click the `black_bot_baf` folder ▸ *Git Bash Here*). The prompt should end in
   `black_bot_baf`.
   ```
   sh scripts/env-lock.sh
   ```
   Type a passphrase (nothing shows), Enter, type it again. Put the passphrase in your password manager —
   there is no recovery. It prints `wrote .env.enc`.
2. **Move the encrypted file by hand — never commit it.** ⚠️ Changed 2026-09-10: the repo is PUBLIC now, so
   `.env.enc` is gitignored and was purged from history (`TODO.md`, the repo-public item). Copy it to the laptop
   over OneDrive / AirDrop / a USB stick and run `sh scripts/env-unlock.sh` there.
   Never run `sh scripts/env-lock.sh` through Claude — it must never see the values.
3. **Leave this Claude Code window open** if you want the Sunday 16:05 wake-up to fire (it lives in the
   session). The laptop can sleep and reconnect; the window on this machine must stay open. If you close it,
   nothing breaks — Sunday's work simply starts when you next open a session and say "continue".
4. Optional: give the **Bots** role *View Audit Log* (so hand-made role changes name who did them) and
   *Send Messages* in voice channels (so temp-voice panels can post) — two clicks in Server Settings ▸ Roles.

## 2. On the laptop (once, ~10 minutes)

1. Install **Git for Windows** (gitforwindows.org) — gives you Git Bash and OpenSSL.
2. In Git Bash:
   ```
   git clone https://github.com/skymitch9/black-bloc.git
   cd black-bloc
   sh scripts/env-unlock.sh
   ```
   Enter the passphrase → `wrote .env`. (Only needed to run the bot locally; skip if you'll only deploy.)
3. Only if you want to deploy from the laptop: `winget install --id Fly-io.flyctl`, open a **new** terminal,
   `flyctl auth login` (browser), then follow **Deploy** in [`runbook.md`](runbook.md). Python/Node only
   if you want to run tests or the mock: `python -m venv .venv && .venv/Scripts/pip install -e ".[dev]"`.

## 3. On the road — what to look at (phone is fine)

| Want to… | Go to |
|---|---|
| See if the bot is up | https://blackbloc.heygabi.ai/health (should say `"ok":true,"ready":true`) |
| See what it has been doing | https://blackbloc.heygabi.ai → **Logs** (every feature page has its own Logs section too; every feature panel has a **Logs** button in Discord) |
| See the team's requests | https://blackbloc.heygabi.ai/requests.html — members file with `/request` in Discord (opens a panel — **File a request**) or sign in and file on that page (they see only Requests). ⚠️ The panel's **list** of requests is staff-only: a member sees File a request, Refresh, the site link and a **Take one back…** select, but not their own requests written out. Set `request_panel_own_list` to `true` (Settings page, or `/settings` ▸ **A setting group…** ▸ request) to give members their list back |
| Give people their own voice rooms | `/voice` in Discord — ONE command that opens a panel, for members and staff alike. Your own room's card has **Rename · Limit · Lock/Unlock · Hide/Show · Bitrate**, then **People…** (let somebody in, keep somebody out, move somebody out, undo any of it), **Region…**, **Hand it over…** and **Forget my settings**. Somebody whose owner has left the room gets **Claim**. Staff get the whole status block plus **Setup**, **Forget a lobby…**, **Turn join-to-create off/on**, **Logs**, an **A channel…** picker for every open room — from which staff can hand ANY room to somebody else, and the displaced owner is DM'd — and the site link. A Lead without the Member role still gets all of that. The old `/tempvoice setup|forget|status|mode` and eighteen `/voice …` subcommands are gone; the post that appears beside a new room is unchanged. Site: https://blackbloc.heygabi.ai/tempvoice.html |
| Change how the bot talks, or what it knows | `/chat` in Discord — ONE staff command that opens a panel. The status block is written out at the top always (mode, the two tiers, answers today, the month's spend, how many notes, and any trouble with the daily read); then **Personality…** (the voice, and two selects — *Turn a mood off…* / *Turn a mood on…* — never one toggle), **Knowledge…** (the notes list, **Write one down…**, **Find…**, and a card per note with **Remove** and **Edit…**), **Settings** with a **Limits…** modal for the five numbers, the two mode toggles, **Logs** and the site link. ⚠️ `/chat status`, `/chat logs`, `/chat settings`, `/chat personality show|set|mood` and `/chat knowledge add|list|remove` are all **gone** (2026-09-04) — every one of them is a button. `chat_status_admin_only` now hides the **spend block**, not the whole command, so a non-admin staffer still gets Knowledge and Personality and is told in words who may read the money. A note Black Bloc wrote itself from the server (`server`) shows neither Remove nor Edit and says why. `chat_panel_minutes` (10) decides how long the panel stays live. Site: https://blackbloc.heygabi.ai/chat.html |
| Store a birthday, or see whose is coming up | `/birthday` in Discord — ONE command that opens a panel. **Set my birthday** is a modal (`MM-DD`, or `MM-DD-YYYY` if you want the year); **Remove**, **Opt out** / **Opt in** and **Refresh** are buttons, and **Look someone up…** shows anybody's stored date. Staff get three more rows on the same panel: a month list, a **Wishes are…** off/shadow/on picker, **Status**, **Clear the birthday role** and **Logs** — and, on somebody's card, **Set their birthday** and **Forget their birthday** (they are DMed when staff forget it). Two switches decide what members see: `birthday_panel_next_for_members` and `birthday_panel_lookup`, both **on** — Settings page, or `/settings` ▸ **A setting group…** ▸ birthday. The old `/birthday set` / `show` / `next` / `list` / `mode` / `status` subcommands are gone |
| Run a poll, or decide one waiting on review | `/poll` in Discord — **one** command that opens a panel. **Create** is a two-step modal (question, `A \| B \| C` options, hours, kind) then a preview with **Post it · Repeat… · Start over · Cancel**; nothing is written until Post it. Pick a running poll on the select to see results, **End** (creator or staff) or **Cancel** (staff). With review on, staff **Approve** / **Deny** from the panel — and a denied poll still has **Post it anyway**. Site: https://blackbloc.heygabi.ai/polls.html |
| Keep an eye on what people post | `/automod` in Discord — **one** staff-only command that opens a panel. It carries the whole of the old `/automod status` (mode, resolved staff, warn threshold, modlog, both exemption lists, how many cases it has acted on and logged) and then **A rule…** for each of the seven rules — its card has one **Turn it on/off** button, **Change the numbers…**, **Log only**, a *What it does…* picker (delete / warn / time out), and for `bad_words` a **Words…** box you fill one word per line, exactly like the website. **Exemptions…** adds a role or a channel with a picker and takes either back off ONE *Watch it again…* select; **Settings…**, **Refresh**, **Logs** and the site link finish the row. ⚠️ `/automod status`, `/automod mode`, `/automod rule enable\|disable\|set`, `/automod exempt add\|remove` and `/automod logs` are all **gone** (2026-09-04) — every one is a control now. **on** is not even offered while `staff_channel_id` is still the test channel or resolves no staff role, and the panel says which of the two is in the way; when it IS offered, arming asks *Are you sure?* first (`automod_arm_needs_confirm`, on by default — turning automod off or back to shadow is always one press). Site: https://blackbloc.heygabi.ai/automod.html |
| Look at what has been done to somebody, and correct it | `/mod [member]` in Discord — **one** staff-only command that opens a panel. The root lists the newest ten cases (the lines `/cases` used to print) with **A case…** to open one, **Whose cases?** to narrow to a member, **‹ Newer** / **Older ›** (each drawn only when there is a page that way), **Everyone's cases** while a filter is on, **Jump to case #…** for a number off a log line, **Refresh**, **Logs** and the site link. A case's card carries **Edit reason…**, **Add a note…** (which says **Edit the note…** once there is one, never both), **Void this case…** and, once voided, **Restore this case** — and the note is for the next moderator, not the member. ⚠️ **Voiding does not undo the punishment**: a voided ban is still a ban, a voided timeout is still running — `/unban` and `/untimeout` are what lift them, and the card says so. A voided case is marked and struck through, never deleted, and staff can always restore it. ⚠️ A voided warn stops counting toward `automod_warn_threshold`. ⚠️ `/case`, `/cases` and `/mod logs` are all **gone** (2026-09-05) — every one is a control now, and the top-level count really drops (36 → 34). The seven bare actions — `/warn`, `/timeout`, `/untimeout`, `/kick`, `/ban`, `/unban`, `/purge` — are UNCHANGED, because they are typed mid-incident, and the panel's footer says where they live. The same four moves are on the dashboard's case drawer. `mod_panel_minutes` (10) decides how long the panel stays live. [`sweeps.md`](sweeps.md) rows 208–218 (lettered until the conductor numbers them). Site: https://blackbloc.heygabi.ai/moderation.html |
| Hand roles out, or run the role menus | `/rolemenu` in Discord — **one** staff-only command that opens a panel. The root lists every menu (name, how many roles, mode, posted or not) with **A menu…**, **New menu**, **Seed the defaults**, **Grants…**, a **Waiting on staff (N)…** button while requests are pending, the mode button, **Logs** and the site link. A menu's card has **Add a role…**, a *Take a role off this menu…* select, **Words…**, **Rules…**, **Ask staff first / Hand it over straight away**, **Post it** (or **Move it…**), **Take it down**, **Hand roles out…** and **Delete it** — each drawn only when the move is actually possible. ⚠️ **Grants… opens as an AUDIT** of every timed role running right now, soonest to end first, with the time left and the end date; **Whose roles?** narrows it to one member and a grant's card carries **Push it back…** and **End it now** — the `/role revoke` four docs used to promise and nothing ever had. ⚠️ The whole `/rolemenu` group (logs, create, add, remove, edit, list, show, showall, post, unpost, assign, unassign, mode, delete, seed-defaults) and the whole `/role` group (logs, grant, extend) are **gone** (2026-09-04) — every one of them is a control now, and the top-level command count really drops (38 → 37). With `rolemenu_mode` **off** the command still opens and says picking is off in words; `rolemenu_panel_minutes` (10) decides how long the panel stays live. Site: https://blackbloc.heygabi.ai/rolemenus.html |
| Catch the spam bots | `/honeypot` in Discord — **one** staff-only command that opens a panel. It carries the whole of the old `/honeypot status` (mode, resolved staff roles by name, the trap channels, how many days of a banned account's messages go with them, the exempt roles, and the banned/logged-in-shadow/failed/ignored tally) over a **What the trap does…** picker (off / shadow / on), a **Roles the trap ignores…** picker whose selection IS the exempt list, **Setup…** (a one-box modal for the channel name, and it is simply not there once a live trap exists), **Forget…** (a picker over every recorded trap — one Discord no longer has is labelled as such, so no id is ever typed), **Exempt nobody** when there is something to clear, **Settings…**, **Refresh**, **Logs** and the site link. ⚠️ `/honeypot status`, `/honeypot setup`, `/honeypot mode`, `/honeypot forget`, `/honeypot exempt add\|remove` and `/honeypot logs` are all **gone** (2026-09-05) — every one is a control now. **on** is not even offered while no staff role resolves, and the panel says so in words, because with no staff exempt one mistyped moderator message is a real ban. The hits table and its per-row **Ban now** stay on the website. Sweeps rows 188–199. Site: https://blackbloc.heygabi.ai/honeypot.html |
| Run the modmail inbox | `/modmail` in Discord — **one** staff-only command that opens a panel. It carries the whole of the old `/modmail status` (whether Black Bloc answers DMs, the mode, the ticket category, the staff channel, the transcripts channel, which staff roles resolve, how many people are blocked, the reconciler's last-ok and last-error, and every open ticket) and then **Setup…**, **Blocked…**, **Snippets…**, **Forget…**, **Logs**, **Refresh** and the site link. With at least one ticket open it also carries **A ticket…** — pick one and the panel becomes that ticket's card, the same embed the sticky card in the channel carries, over **Reply · Reply as Staff · Private note · Close… · Back**, so a ticket can be answered without going to its channel at all (2026-09-05 follow-up). **Setup…** points each of the three places with a channel picker, picks channel-or-thread mode, and turns answering DMs on or off with ONE button that names what it will do. **Blocked…** lists who is blocked, **Unblock them** the one you pick, and **Block someone…** reaches anybody at all through a member picker plus a reason box. **Snippets…** has **Add one…**, **Change it…** and **Remove it** (which asks first) — ⚠️ adding a name that is already taken is refused **in words naming Change it…**, because `overwrite:true` no longer exists. ⚠️ The whole `/modmail` group (logs, block, unblock, blocked, mode, forget, status, settings) and the whole `/snippet` group (add, remove, list) are **gone** (2026-09-05) — every one is a control now, and the top-level command count really drops (36 → 35). ⚠️ `/modmail` can never be hidden when modmail is off, because the panel is the Discord door to turning it on. `modmail_panel_minutes` (10) decides how long the panel stays live. [`sweeps.md`](sweeps.md) rows 200–207 and 245–251. Site: https://blackbloc.heygabi.ai/modmail.html |
| Answer a modmail ticket, or decide how staff answer them | The ticket itself carries **one card at the bottom** with **Reply · Reply as Staff · Private note · Close…**, and the card jumps back under the newest message every time anything is written into the ticket. ⚠️ **`/areply`, `/note` and `/close` are gone** (2026-09-05, Build B) — they are those four buttons — so the count drops again (33 → 30). **`/reply` stays**, with its optional `ticket:` for naming a ticket from the test channel. **`/modmail` → Setup… → Reply style…** decides whether a plain message typed in a ticket still reaches the member: **typing** and **both** relay it as they always have, **buttons** does not (nothing happens at all — no relay, no ✅), and `=note` is still a private note in all three. ⚠️ **`/modmail` → Try a fake ticket** is how you make that choice: it opens a **practice** ticket — a private thread on the test channel with a real card — where you can speak as the member, try all three doors and end it, and **nobody is ever DMed**. Closing it files a transcript marked PRACTICE. ⚠️ **In a REAL ticket under test mode the card appears in `#mute-me-bot-test-spam`, not in the ticket** — that is the guard redirecting, not a bug, and it is why the practice ticket exists. Migration: schema 29 → 30. [`sweeps.md`](sweeps.md) rows 219–230 |
| Change any setting, or put Black Bloc's own face back | `/settings` in Discord — **one** staff-only command that opens a panel, and the last `app_commands.Group` in the estate is gone with it. The root reads every feature's mode **read-only** (each line names the `/command` that changes it), says how many commands are hidden and how many settings this server has moved off the default, and offers **Turn a feature back on…** (only when something IS hidden), **A setting group…** → a key card with the right editor for its type and **Put the default back**, **Roles & channels…** (the four channel keys that decide who is staff — drawn only for Manage Server while `settings_core_keys_admin_only` is on, and the panel says in one line who it is for), **How Black Bloc looks…** (About Me, status, skin tone and **Re-apply presence**), **Panels & commands…** (the hiding switch, every panel's minutes, the operator-read log, the hosting bill), **Log levels…** (pick a feature, then the two levels it is not on), **Logs**, **Refresh** and the site link. ⚠️ `/settings show`, `set`, `set-role`, `set-value` and `clear` and the whole `/presence` group are **gone** (2026-09-05) — every one is a control now, and the top-level count drops **30 → 29 with ZERO groups left**. ⚠️ Putting the default back on `staff_channel_id` asks first, because its default is `#mute-me-bot-test-spam` and that quietly disarms automod and the honeypot. `settings_panel_minutes` (10) decides how long the panel stays live. [`sweeps.md`](sweeps.md) rows `S1`–`S12` (lettered until the conductor numbers them). Site: https://blackbloc.heygabi.ai/settings.html |
| Test something | [`sweeps.md`](sweeps.md) — **350** rows in priority order (counted 2026-09-11; it said 261 until then), each with what to expect (+ the detailed phase 1–8a scripts). The newest batch is **343–350**, the Where-shorthand build (v108); 341–342 the test-room timer; 338–340 the masked link and Open link button; 231–244 the `/settings` panel's, 245–251 the modmail ticket-card follow-up's, 252–261 the self-test's |
| Make the bot test itself | `/settings` ▸ **Self-test…** ▸ **Run the self-test** in Discord, or the **Self-test** card on https://blackbloc.heygabi.ai/health.html#sect-selftest — and it runs on its own at every boot (`selftest_on_boot`, true). It checks every channel/role setting still resolves with the permissions the feature needs, POSTS all 18 panel cards and six announcement embeds into `#mute-me-bot-test-spam` as real messages (`selftest_channel_id`), and reads every page the dashboard reads. ⚠️ **A minute later it deletes every message it posted** (`selftest_purge_minutes`, **1** minute — was 5 until 2026-09-10, re-confirmed 2026-09-11; **Purge now** on the same card does it early) — the rows stay on the Logs page under the **Test** chip, which the default **Everything** view leaves out on purpose. The deploy log carries one line: `selftest: N ok, M failed, K messages posted (purge in 1 min)` — the last few deploys read `107 ok, 0 failed, 24 posted`. A second run while one is going is refused in words. Migration: schema 30 → 31. [`sweeps.md`](sweeps.md) rows 252–261; [`testing.md`](testing.md) for `tests/live/` against the deployed API |
| Turn a feature off and hide its command | dashboard → **Settings** → `<feature>_mode` → **off**. Within about a minute that feature's one slash command disappears from Discord — `/youtube`, `/poll`, `/golive`, `/voice` and eleven others. ⚠️ **`shadow` is not `off`** and does not hide anything. To get it back: `<feature>_mode` → **on** (dashboard, or `/settings` ▸ **Turn a feature back on…** in Discord — `/settings` can never be hidden). To stop the hiding altogether, set `hide_commands_when_off` to `false` and every command returns while the features stay off. [`sweeps.md`](sweeps.md) rows 183–187 |
| Link your Twitch, or stop your streams being announced | `/golive` in Discord — **one** command that opens a panel. Members get **Link my Twitch channel** (a one-line modal; **Change my channel** afterwards, prefilled), **Unlink**, and **Stop announcing my streams** / **Announce my streams again** — exactly one of the pair is ever drawn. Staff get the whole of the old `/golive status` written into the same embed (mode, stream end, channel, cooldown, twitch polling, last good poll, last poll error, the counts, and who is live right now) plus **Logs**, **Streamers…** (unlink or opt out somebody else, the same as the Go-live page does it), **Preview an announcement…** (ephemeral, never pings, never posts) and an **off / shadow / on** select. ⚠️ `/twitch link`, `/twitch unlink`, `/golive optout`, `/golive optin`, `/golive status`, `/golive mode`, `/golive test` and `/golive logs` are all **gone** (2026-09-03) — every one of them is a button now. `golive_panel_minutes` (10) decides how long the panel stays live. ⚠️ **In test mode the panel SAYS so**: if `golive_channel_id` is not `#mute-me-bot-test-spam`, the channel line tells you that is why nothing real is posted |
| Put an event on, or decide one | `/event` in Discord — **one** command that opens a panel. Members get **Propose an event**, **My time zone** and, once they have one on the go, **Call one off…**. ⚠️ **Propose an event opens a DRAFT PANEL, not a form** (2026-09-10): Day / Hour / Minute / How long are dropdowns, **Title & details** is a box that can never refuse you — whatever you typed stays on the panel, which says what is still needed — and **Submit** only appears once everything passes. **My time zone** is a dropdown of the zones in `timezone_choices` (24 of them, each showing the time there now) with **Other — type it…** at the bottom for anywhere else. `/raidtrain` ▸ **Start a raid train** works the same way. Staff get the counts, the open list, **Pick an event…** → Approve / Deny / Call it off, plus **Settings** and **Logs**. ⚠️ `/event create`, `/event list`, `/event cancel`, `/event settings`, `/event logs` and the whole `/timezone` group are **gone** (2026-09-03) — everything they did is a button. The panel's **list** of your own events is staff-only; set `event_panel_own_list` to `true` to give members theirs back, and `event_panel_minutes` decides how long the panel stays live (10 by default) |
| Type a link into an event's **Where** without typing a link | The Where box turns shorthand into the real thing before it is saved: `ttv/skyaiva`, `ttv skyaiva`, `yt skyaiva` and `yt @skyaiva` all become `https://twitch.tv/skyaiva` / `https://youtube.com/@skyaiva`, and a bare host (`twitch.tv/skyaiva`, `kick.com`, `discord.gg/baf`) is treated as a link wherever it is drawn — including on events stored before this build, with no migration. The shorthand table is a setting, **`events_where_link_aliases`** (dashboard → **Settings** → **Events**, or `/settings set-value`), so you can add your own; `ttv twitch yt youtube kick tiktok ig instagram x twitter discord` come as standard. ⚠️ Black Bloc never guesses: an unknown alias, three parts, or a TLD that is not at least two letters (`8.30`, `e.g`) all stay as the words you typed. **The link is also opened once before it is kept** — `events_where_link_check` is **warn** by default, which keeps the link and puts `⚠️ that page answered 404 — check the name` on the draft's Where line (never on the card, the announcement or the stored event); **refuse** makes the box say so in a sentence and keeps the old place; **off** checks nothing. `events_where_link_check_seconds` is 1–3, default **2**, because Discord closes a box that has not answered within three. ⚠️ **Measured 2026-09-11 00:3x: Twitch, Kick, TikTok, Instagram, X and Discord answer 200 for ANY name** (single-page apps — the 404 happens in your browser, not in the reply), so a misspelt name on any of those passes the check silently. What it DOES catch: a typo'd host (`twich.tv`) and a wrong **YouTube** handle (`youtube.com/@…` genuinely 404s — and `@skyaiva` is one that does not exist, so `yt skyaiva` shows the note until you type the handle YouTube actually has). [`sweeps.md`](sweeps.md) rows 343–350 |
| Stop test events' review rooms piling up | `events_test_retention_minutes` (dashboard → **Settings** → **Events**, or `/settings set-value`) — **5** by default: while Black Bloc is in **test mode**, a finished, denied or called-off event's review room is deleted that many minutes after it is settled, instead of the seven days `events_channel_retention_days` gives a real one. ⚠️ **The sweep only runs every five minutes, so a room actually goes between 5 and 10 minutes after the decision** — that is not a fault. ⚠️ A **denied** or **called-off** event now counts from the **decision**, not from the start it was never going to have: refuse a proposal today for next month and its room goes today. A **done** event still counts from when it ended. Bounds 1–1440; both refusals are sentences. [`sweeps.md`](sweeps.md) rows 341–342 |
| Apply for something, or run the forms | `/apply` in Discord — **one** command that opens a panel. Members get **Apply for…** (the same form modal as before), their own applications written out, and **Take one back…**. Staff get **Pick an application…** → Approve / Deny, **A form…** → the form card (**Edit… · Questions… · Close it/Open it · Post the Apply button · Roster · Fill it in · Delete**), plus **New form**, **Find #…** (any application by number, settled ones included), **Settings** and **Logs**. ⚠️ The whole `/applications` group and `/apply start` / `status` / `withdraw` are **gone** (2026-09-03) — everything they did is a button. With `applications_mode` **off** the command still opens and says so in words; `applications_panel_own_list` (**on**) decides whether members see their own applications, and `applications_panel_minutes` (10) how long the panel stays live |
| Run a raid train | `/raidtrain` in Discord — **one** member-visible command that opens a panel. Everyone gets the trains coming up and **A train…** → that train's card, which is the same lineup the room reads. On the card: **Take an hour…** (the only claim door, and it is simply not there when you have no Twitch link, when the cap is reached or when the train is not open — with a line saying which), **Give back slot #N** when you hold one hour, **Give an hour back…** when you hold more, and **My slots…** on the root for the hours you hold across every train. Organizers — the `raidtrain_organizer_role_id` role **or** staff — also get **Start a raid train** (the same five-field form), **Put somebody in…**, **Take somebody off…**, **Change two slots round…**, one **Lock the lineup** / **Open it for sign-ups** button (never both, and neither while a train is running) and **Call it off…**, whose reason modal IS the confirmation. Staff get the sweep's health written into the panel always, **Mode…** (off / shadow / on), **Setup…** — where **Clear…** can now take the organizer or ping role back OFF, which only the website could do before — **Logs** and the site link. ⚠️ The whole `/raidtrains` group and all twelve `/raidtrain …` subcommands are **gone** (2026-09-04); `/raidtrain` still opens while raid trains are **off**, because **Mode…** is now the only door back to `on`. `raidtrain_panel_minutes` (10) decides how long the panel stays live. Site: https://blackbloc.heygabi.ai/events.html#sect-raidtrains |
| Keep a list of people instead of handing a role out | https://blackbloc.heygabi.ai/rolemenus.html#applications → **Applications**. A form's role is now optional: leave **Role it hands over** on **No role — keep a list** and an approval stores the record, DMs the person and gives nothing. Each form gets an **Approved for …** foldout — everyone who was said yes to, their Twitch login where they have linked one, and **Copy as text** for pasting into the official team page. Staff take somebody off with the roster's **Take off the list**, or in Discord with `/apply` → **A form…** → **Roster** → **Take somebody off…**; they are DMed the reason. A form that DOES hand a role over is left with `/rolemenu` ▸ **Grants…** ▸ the grant ▸ **End it now** as the way off (`/role revoke` never existed), and staff can put a denied or removed application back with **Approve after all** / **Put them back on the list** |
| Turn a feature on/off | Dashboard → that feature's page → the ON / SHADOW / OFF switch (or **Settings**) |
| Quiet the Discord log channel | Settings → `<feature>_log_level` (off / important / all) |

Everything still runs in **test mode** (only `#mute-me-bot-test-spam`, DMs, and the temp-voice channels it
made). Lifting it is a Fly secret (`TEST_MODE=false`) — your call, never Claude's.

## 4. If something looks wrong

1. Screenshot it, note the time (Phoenix).
2. Check [`runbook.md` ▸ Common failures](runbook.md) — most things there are "expected in test mode".
3. Paste the screenshot + time to Claude when you're back; the Fly logs around that minute + the Logs page
   are enough to diagnose. Nothing on the sweeps list is destructive.
4. Genuine outage (health page dead for more than a minute): in a terminal with flyctl,
   `flyctl machine restart 85e744c4d959d8 --app black-bloc`.

## 5. When you're back (Sunday night)

1. Open the Claude Code session (or a new one — it reads `docs/` first) and say **continue**. It will show
   you what landed while you were gone (`docs/TODO.md` top entries, `docs/DONE.md`, `docs/deploys.log`).
2. ~~Decide one thing: **keep `docs/` tracked in git, or purge it from history again**.~~ ✅ **DECIDED
   2026-08-31 — keep it tracked** ("actually lets keep it tracked", commit `1eb8870`), permanently.
   🔴 And the repo itself is **PUBLIC** since 2026-09-10 20:06, so `docs/` is world-readable:
   secret NAMES only, never a value, anywhere under it.
3. Work the [`sweeps.md`](sweeps.md) list top-down; tell Claude each result.
4. The Requests page is your backlog — approve / plan / assign from there.
