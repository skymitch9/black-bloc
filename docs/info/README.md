# info/ — how Black Bloc works, and why it is built this way

> **Audience:** Claude sessions and the owner. **Status:** TRACKED (owner,
> 2026-08-31 — was local-only until then).
> Last verified: **2026-09-11 09:45** — **docs-wide staleness pass: every row in this table was
> checked against `../DONE.md` and `../deploys.log`, the first time the whole table has been
> re-read since 2026-08-31.** **Measured first:** every `.md` file in `info/` has exactly one row
> and every row points at a file that exists (**66** design/reference docs + `mock-direction-a/`,
> checked by script, both directions). **Fourteen rows carried a status that landing had already
> overtaken, and are FIXED:** `operator-read-design.md` said the feature was *inert until the
> owner mints the secret* — minted 2026-09-06 and **Deployed with v97**; `panels-program.md` said
> *PLANNING* — **COMPLETE** since v84; `personality-pool-design.md` said *DESIGN, not built* —
> **both halves LIVE** as v90/v91; `site-restyle-design.md` said *build after B4–B8* — **both
> slices live 2026-08-31**; `logs-buttons-design.md`, `recurrence-web-create-design.md`,
> `poll-drafts-design.md` and `loop-guard-design.md` carried **no status at all** (v96, v96, v94,
> v97 — and KI-24 closed with the last); `phase9`, `phase14`, `phase15`, `phase8b` and the
> `phase1–8` group row carried no landing fact; `cutover-plan.md` now says plainly that it is
> **NOT STARTED**. Three rows had a stale NUMBER: `test-suite-profile.md` (a 5188-test snapshot,
> **5546** today), `feature-list.md` (the F17 row it asked for now exists), `review-checklist.md`
> (34 items re-counted; `CLAUDE.md` said 33 and was one behind — both say 35 since 11:00). ⚠️ **NOT checked:** the BODY of
> any design doc — other agents refreshed those in parallel, and these rows were rebuilt from
> `DONE.md`/`deploys.log` rather than from their headers, so the two agree by construction; no
> browser rendered anything and nothing here met Discord.
> Before that, **2026-09-11 00:38** — the `where-picker-design.md` row's **follow-up 4** clause (a shorthand becomes a link, and the link is tried before it is kept) flipped to ✅ **LIVE v108** 00:37, merge `73e2e44` of `where-smart` — ⚠️ **never seen in Discord**; sweep rows 343–350 are the owner's; the review probed the real link check live (measurements in the design foot, H16/H17). `code-notes.md` gained a `## Where follow-up 4` block keyed by NAME. ⚠️ **NOT checked:** every other row in this table, and no browser rendered anything. Before that, **2026-09-10 23:45** — the `where-picker-design.md` row's **follow-ups 2+3** clause
> (a typed link that looks like a link; test rooms in minutes and denied rooms counted from the decision),
> ✅ **LIVE v107** 23:41, merge `ac43a20` of `where-links` — ⚠️ **never seen in Discord**; sweep rows 338–342
> are the owner's. `code-notes.md` gained a `## Where follow-ups 2 and 3` block
> keyed by NAME. ⚠️ **NOT checked:** every other row in this table, and no browser rendered anything. Before
> that, **2026-09-10 17:45** — the `where-picker-design.md` row's **follow-up** clause (a channel
> AND a link) flipped 🔨 BUILT → ✅ **LIVE v106** 17:41 (merge `6c10b9d`; never seen in Discord — sweep rows 336–337
> are the owner's), and `code-notes.md` gained a `## Where follow-up` block keyed by NAME. ⚠️ **NOT checked:** every
> other row in this table, and no browser rendered anything. Before that, **2026-09-10 17:14** — the
> `where-picker-design.md` row flipped 📐 DESIGN → 🔨 BUILT → ✅ **LIVE**
> (merge `3205c0f`, v105 17:09; ⚠️ never seen in Discord — sweep rows 329–335 are the owner's), and
> `code-notes.md` gained a `# The "Where?" picker` section keyed by NAME. ⚠️ **NOT checked today:** every
> other row in this table. Before that, **2026-09-03** — the `operator-read-design.md` row was ADDED (the build's own
> branch; ⚠️ BUILT but the feature is **inert until the owner mints `OPERATOR_READ_TOKEN`**), and
> `code-notes.md` gained an `# Operator read token` section. Earlier the same day, the
> `applications-panel-design.md` row flipped to **SHIPPED**
> (merged `853776c`, live v66 15:00, synced 42 measured on the boot log; wave 1 of the panels program
> is COMPLETE — `panels-program.md` §4 rows say v63/v64/v65/v66), and `code-notes.md` gained an
> `# Applications panel (wave 1)` section. Earlier the same day that row was added (wave 1's fourth
> design, written against `9891f71` after the no-role merge; three owner forks open), and the
> `applications-no-role-design.md` row flipped to **SHIPPED**
> (merged `9891f71` 12:40, live v62 12:48, schema 28; 5 deviations), and
> `code-notes.md` gained an `# Applications, no-role pass` section. Earlier the same day the
> `requests-check-design.md` row flipped to **SHIPPED**
> (merge `44170f4`, release v61 at 12:29, schema 27; ~12:35). Earlier the same day the three
> wave-1 panel design rows were added (~12:15: `events-panel-design.md`,
> `polls-panel-design.md`, `birthdays-panel-design.md`, each reviewed against
> `panels-program.md` §2). Earlier the same day the `applications-no-role-design.md` row was
> added (~12:20, BUILDABLE), and the `requests-check-design.md` row itself (~12:05,
> BUILDABLE). Earlier the same day the `panels-program.md` row gained its wave-0 clause once
> `black_bloc/panels.py` was built (`feat/panels-library`). Earlier the same day that row was
> added (11:10). Earlier the same day
> the `requests-panel-design.md` row flipped to SHIPPED (merge
> `ba5cb99`, 11 deviations). Earlier the same day the `requests-embeds-design.md` row flipped to SHIPPED (merge
> `355d6e9`, deployed `70a6720`, landing one-off run; 14 deviations). Earlier the same day it flipped to
> BUILT-not-merged and was rewritten to name its Deviations foot and the landing one-off.
> Earlier the same day that row was rewritten to match
> the doc (review state, seven looks) and flipped to BUILDABLE at the double-logging fix
> landing (`df393ab`); checklist count 33 → **34**. Earlier the same day the phase
> 17/18/19 + requests-states rows flipped to SHIPPED at the 17 → 18 → 19 landing and a
> duplicate `phase19-design.md` row was removed (one file, one row). Before that, 2026-08-31 — every file in `info/` was listed and matched
> against the rows below; the missing `mock-direction-a/` row was added and the
> `review-checklist.md` item count corrected (20 → **33**, counted today).
> ⚠️ **NOT checked on 2026-08-31:** that every file in `info/` still has a row (✅ checked by
> script 2026-09-11, both directions); the contents of the phase design docs themselves.

| File | Answers |
|---|---|
| [`guides-design.md`](guides-design.md) | **Guides — one web page per goal, staff-editable, real screenshots, live values** (2026-09-15, 📐 **DESIGN, not built, not dispatched**; mock https://claude.ai/artifact/C6MGnYLSdSyDHL42y729YA): a guide is rows (`guides` / `guide_steps` / `guide_faults` / `guide_facts` / `guide_media`, schema 34 → 35) seeded once off `access/sweeps.md` in the IKEA voice (one **bold** control per step, one Expect line, a warn-only linter) and edited in place on `guides.html` by staff with the Settings page's docked save bar; member-gated through `member_dependency` (owner: "server level access"); screenshots are real captures a Claude-in-Chrome session shoots off the cards the self-test posts (owner: "You can screenshot discord in browser mode"), drawn mocks only for screens a capture cannot reach, both marked stale by `release.json` written at deploy from a `FEATURE_PATHS` diff; up to four "Right now" facts per guide read from the registry and fixed probes; `/help` gains a masked `guide` link per line + one button; "Something's off" files a request. Six `guides_*` keys. F-G1 DECIDED "A" (no Discord editing door); forks F-G2, F-G3 open. Two Opus dispatches, 200–280k + 220–320k |
| [`operator-read-design.md`](operator-read-design.md) | **Operator read token** (2026-09-03, ✅ **LIVE** — the secret was minted 2026-09-06 13:34 and went Deployed with **v97**; the first operator read ever answered at 13:54, `pytest -m live` is **58 passed / 1 skipped** against v98/v99, and the read is bounded by the dashboard's own 300/min bucket since **v99** `88e0242`. This row said "inert until the owner mints the secret" until 2026-09-11): why a session could not read live state (`flyctl ssh` + the permission classifier), the 13 decisions — bearer on `GET`/`HEAD` only, unset = no door, a per-IP bucket, no `sessions` row, one `web.operator.read` row per request, a third `via` word — the exact refusal words, the security argument, the two auth GETs that DO write and are unreachable this way, and a `## Deviations` foot of 6. Operating it: [`../access/operator-read.md`](../access/operator-read.md) |
| [`panels-program.md`](panels-program.md) | **Panels over slash commands — the program for the rest of the app** (2026-09-03, ✅ **COMPLETE** — every wave landed, the last of them `/settings` as **v84** 2026-09-05, taking the tree to **29 top-level commands and ZERO `app_commands.Group`s**, still true at v108; the item moved whole to `../DONE.md` 2026-09-04. This row said "PLANNING" until 2026-09-11): the 17 invariants every panel inherits from `/request` (P1–P17, each traced to a deviation or a review finding), the measured command inventory (~177 subcommands over 44 top-level commands → ~21 commands), wave 0 = extract `black_bloc/panels.py` from the requests cog — **BUILT 2026-09-03**, so §4 is the reference for `Panel`, `retire`, `answer`, `still_staff`, `db_ready`, `capped_placeholder`, `panel_minutes` and the generic `NoteModal` every later panel imports — four waves, the three owner forks (mod commands, modmail's in-thread replies, `/settings`). Every `<feature>-panel-design.md` links here instead of restating the pattern |
| [`events-panel-design.md`](events-panel-design.md) | **Wave 1 — `/event` is ONE command — ✅ SHIPPED in v64** (`e670542`, 2026-09-03 14:05; not run against Discord by eye): member panel (Propose via the unchanged 5-field modal, `My time zone` as a one-line modal beside it — 598 zones cannot fit a 25-option select, `Call one off…` over your own open events), staff panel (counts, the open list, a 25-cap pick select, a Settings sub-panel, Logs), the card renders only `TRANSITIONS`' moves; no `Mark it done` (loop-only, no shared path). **`commands synced` drops 44 → 43** — the program's first real drop, `/timezone` retired whole (owner I2, "Remove it"). The shared events layer moved from the cog into `black_bloc/events.py`. `Call it off` carries an optional DM'd note; `denied` gains `Approve after all`. Sweep rows 73–79; see its `## Deviations` foot |
| [`recurrence-web-create-design.md`](recurrence-web-create-design.md) | **Create a recurring poll from the website** (owner 2026-09-06: "2. A"; ✅ **LIVE v96** `c915ade` 2026-09-06 11:39): `POST /api/polls/recurrences` reusing `poll_plan` → `store_poll(RECURRING)` → `save_recurrence(via=website)`, cadence validated before any row exists, a Repeat block on the Polls page's create form. Routes 149 → 150 |
| [`loop-guard-design.md`](loop-guard-design.md) | **KI-24 loop guard** (owner 2026-09-06: "Build it a"; ✅ **LIVE v97** merge `689eff5` 2026-09-06 13:52 — **KI-24 is CLOSED**): one `black_bloc/loops.py:wait_ready` behind all 14 `before_loop`s so a failure there reaches the cog's `@loop.error` (Health tab, restart); bundled with `Core.cog_load` guarding on `db.is_connected` and `POST /api/polls` refusing `409 polls_off` |
| [`logs-buttons-design.md`](logs-buttons-design.md) | **The Logs button's two knobs as buttons** (owner 2026-09-06: "3. B"; ✅ **LIVE v96** merge `42d2e6e` 2026-09-06 11:39): `Show more` + `Important only`/`Show everything` under every feature's Logs list, editing in place; `LogsPanel(Panel)` in `black_bloc/logs_panel.py`; keys `logs_count` / `logs_important_only`; the 18 `send_logs` call sites unchanged |
| [`when-picker-design.md`](when-picker-design.md) | **The "When?" picker** (2026-09-10, ✅ **SHIPPED v100** 16:00, merge `1f35f28` — ⚠️ **not yet seen in Discord**; 12 deviations at its foot, sweep rows 323–327): the owner typed `2026-09-11` into `Start — YYYY-MM-DD HH:MM`, was refused, and lost the title, the description and the location with it. `/event` Propose and `/raidtrain` Start are now DRAFT PANELS — Day / Hour / Minute (and How long) dropdowns from the shared `black_bloc/when_picker.py`, a text modal that never refuses (what is typed lands on the draft and the panel says what is still needed, in the sentences `checked_fields` already said), and a Submit / Start that renders only once everything passes. `My time zone` is a `ZonePanel` dropdown of `timezone_choices` plus `Other — type it…`, storing through the same `set_zone`. Five keys, all in the Events group: `default_timezone`, `timezone_choices`, `time_step_minutes`, `events_default_minutes` and `events_scheduled_name_template` (the owner’s `{title} Feat. BaF` calendar name). `EventModal`, `TrainModal` and `Events.submit` are gone |
| [`where-picker-design.md`](where-picker-design.md) | **The "Where?" picker** (2026-09-10, ✅ **LIVE v105** 17:09, merge `3205c0f` — ⚠️ never seen in Discord, sweep rows 329–335 are the owner's; 10 deviations at its foot; the channel-AND-link follow-up is LIVE v106): the owner asked for a Where "like a real discord event" (16:11). `/event` Propose has a fifth **Where** button → a `WherePanel` with Discord's own `ChannelSelect` (voice / stage / text), an **Other** modal for a typed place or Twitch link, a **Clear** that renders only when something is set, and **Back**; `EventTextModal` lost its Where box. `events` gained `where_kind` + `where_channel_id` (schema 33 → **34**, no backfill — an old `location` reads as the `other` kind), and one `Where` NamedTuple travels through `read_where` / `where_line` / `checked_fields` / `create_event` / `update_event`. A voice or stage pick makes a real voice/stage scheduled event with Discord's Join button; a text pick or Other stays external; a channel that has gone falls back to `Ask in the server` and logs `event.where_channel_gone`. Staff get the same button on the review card (`set_where`, one `event.edited` row); the website's editor gets a grouped three-way `<select>` fed by the cached `/api/ref/channels`, and `checked_where` refuses an unknown kind or a channel this guild has not got, in words. **No settings key — nothing to decide.** **The `## Follow-up` foot (a channel AND a link together) is ✅ LIVE v106 17:41 (merge `6c10b9d` of `where-link`; ⚠️ never seen in Discord)** — `Where.text` now means something for every kind, the panel's modal button is always there (`Link or place (optional)…` with a channel set), the Where line reads `<#id> · link`, and the link is appended to the scheduled event's description behind the new bool key `events_where_link_in_description` (keys 197 → 198, schema still 34). Its departures are the `## Follow-up deviations` foot; sweep rows **336–337**. **The `## Follow-up 2` and `## Follow-up 3` feet are ✅ LIVE v107 (23:41, merge `ac43a20` of `where-links`) — ⚠️ never seen in Discord** — a typed link now renders masked (`where_link` / `where_shown` / `where_line(linked=)`) with an **Open link** button on the review card, while `knowledge.event_where` and the scheduled event's description keep the bare url; and `swept_anchor` counts a DENIED or CANCELLED review room from `decided_at` rather than a start that never happened, with the new int key `events_test_retention_minutes` (default **5**, 1–1440, keys 198 → 199) deciding the window while the test-mode guard is installed. Departures: the `## Follow-up 2+3 deviations` foot; sweep rows **338–342**. **The `## Follow-up 4` foot is ✅ LIVE v108 (2026-09-11 00:37, merge `73e2e44` of `where-smart`) — ⚠️ never seen in Discord** — `where_link` also recognises a bare host (judged at RENDER time, so rows stored before the build become links with no migration) and the new `where_typed` turns `ttv/skyaiva`, `ttv skyaiva` and `yt @skyaiva` into the finished href at ENTRY, through the staff-editable `events_where_link_aliases` table; the new module `black_bloc/linkcheck.py` opens the link once before it is kept (`events_where_link_check` off/warn/refuse, default **warn**, bounded by `events_where_link_check_seconds` 1–3, default **2**; keys 199 → **202**), warn putting `EventDraft.where_note` on the draft's Where line and nowhere else while refuse answers the modal in words. Schema still 34. Departures: the `## Follow-up 4 deviations` foot; sweep rows **343–350**. |
| [`events-rooms-design.md`](events-rooms-design.md) | **Event rooms** (2026-09-11, ✅ LIVE **v110** 10:14, merge `9c6201d`; ⚠️ never seen in Discord — sweeps 351–359 are the owner's): the event's announce / go-live / new ended post land in its OWN review room (the room is `guard.own_channel`ed at creation and re-owned on reconcile — today it never was, which is why everything fell into the spam channel), a staff-only **Delete this room** button in every room that also cancels an open event with a DM'd reason, and the sweep logs `event.room_forgotten` instead of keeping a dead id. Four keys (`events_posts_where` room/announce/both, `events_room_delete_who`, `events_approver_role_id`, `events_room_notice`), 202 → 206; schema stays 34; retention numbers unchanged (owner: 5 min after the end is test mode only). Sweeps 351–359 at its foot; `## Deviations` holds the build's 11 departures (no `event_lock` in `delete_room`, `room_forgotten` on the sweep only, a Rooms… settings page). |
| [`poll-drafts-design.md`](poll-drafts-design.md) | **Saved poll drafts** (owner 2026-09-06: "B but only save 1 draft per person max"; ✅ **LIVE v94** merge `8405bea` 2026-09-06 11:13, deployed by the owner): `poll_drafts` table keyed on (guild, user), the panel's Save/Resume/Discard moves, staff draft pick + discard with a DM'd reason, `poll_drafts` / `poll_draft_days` keys, `DRAFT` leaves the poll status tables. Schema 32 → 33 |
| [`polls-panel-design.md`](polls-panel-design.md) | **Wave 1 — `/poll` is ONE command** (2026-09-03, ✅ **SHIPPED in v65** (`d13e1a4`, 2026-09-03 14:14); built on `worktree-agent-aa735ab092d13477d`, 9 deviations at its foot; never run against Discord by eye): ⚠️ corrects `panels-program.md` §3 — polls has a genuine member half (`results`/`list`/`recur list` ungated, creator may `end`, `create` when `poll_who_can_create=everyone`), so the panel splits on `is_staff`; picking a poll IS the results; create is a 5-Label modal (`RadioGroup` kind, `CheckboxGroup` switches) → preview → `Post it`, nothing written before; recurrence card with Pause/Resume/Delete through five new module-level functions the dashboard routes are pointed at too; `denied → open` via staff `Post it anyway` (settled by the staff-final-say rule). `poll_panel_minutes` (10) + `poll_creator_may_end` (True — the owner's F-B1 answer). Sweep rows 80–86 |
| [`birthdays-panel-design.md`](birthdays-panel-design.md) | ✅ **SHIPPED in v63** (`616adb3`, 2026-09-03 13:58; merged `58974e1` after Fable review, 3500 tests, ruff clean, `commands synced` **44 unchanged** measured on the boot log; 14 deviations at its foot, F-B1 answered **True/True**; by-eye sweeps 87–93 still owed). **Wave 1 — `/birthday` is ONE command**: row 0 from a `(has_date, opted_out)` button table, a `UserSelect` lookup, a 13-option month list, a mode select, Status/Clear role/Logs — a staff panel fills all five rows; one `DateModal` (`MM-DD` or `MM-DD-YYYY`, `parse_birthday_input` new, then today's exact `date_problem`/`year_problem` sentences); the person card gives staff `Set their birthday` and `Forget their birthday` (the Discord path to the site's existing `DELETE`, per staff final say); seven cog functions the website imports keep their names. Keys: `birthday_panel_minutes`, `birthday_panel_next_for_members`, `birthday_panel_lookup` (default = F-B1). Five strings and `report_lines` name retired commands and are rewritten. Sweep rows 87–93 |
| [`applications-panel-design.md`](applications-panel-design.md) | **Wave 1 — `/apply` is ONE command** (2026-09-03, ✅ **SHIPPED in v66** `853776c` 15:00, synced 42 measured on the boot log; built on `worktree-agent-abf063b9177e02f17`; all three owner forks decided — `/apply` as the name, Visible when the mode is off, and the Questions sub-panel built; 15 deviations at the foot; not run against Discord by eye): two groups (`/apply` member, `/applications` staff) and **seventeen** subcommands collapsed into one member-visible command — `commands synced` **43 → 42**, measured. The queue as a 25-capped select, the application card's buttons from `CARD_BUTTONS` keyed by status × role-or-list, the form card with Edit / Questions / Roster / Post the Apply button / Close / Delete sub-panels, Settings and Logs; `Find #…` reaches settled rows. Staff final say is `denied`/`removed` → `approved` (`reinstate` + `restore_application`); the member keeps their own list (`applications_panel_own_list`, default True — the one new key). The five duplicated form writes now go through one function each with `via` (checklist 34, asserted by row COUNT). Sweep rows 94–102 |
| [`golive-panel-design.md`](golive-panel-design.md) | **Wave 2 — `/golive` + `/twitch` become ONE command** (2026-09-03, ✅ **SHIPPED v70 `0aeed72` 17:25** — merged after Fable review, 8 deviations at its foot; boot measured `commands synced` 41; **not run against Discord by eye**, sweeps 109–117): eight subcommands over two groups collapse into one member-visible command — `commands synced` **42 → 41**, measured through `tests/test_bot.py`. Member rows are Link/Change/Unlink + Stop/Announce again + Refresh/site link from a `panel_buttons(linked, opted_out, staff)` table proved by a parametrised test; staff add the eleven status lines as the embed itself (no Status button), Logs, a Preview select (ephemeral, no ping, never posts, `_post` asserted unreachable), the off/shadow/on mode select and the **Streamers…** sub-panel (owner fork I2 = build it) that reaches another member's link through the same functions the site routes call. **No Settings sub-panel** — the site's Go-live page owns the other twelve keys. The five duplicated `note()` logs are deleted and the routes pass `via=VIA_WEBSITE`; the tests assert the row COUNT (checklist 34). One key: `golive_panel_minutes` (10). Sweep rows 109–117 |
| [`voice-panel-design.md`](voice-panel-design.md) | **Wave 2 — `/voice` + `/tempvoice` become ONE command** (2026-09-03, ✅ **SHIPPED in v73** `4d64b36` 22:08, synced 38 measured on the boot log; built on `worktree-agent-a386d425f5527fe95`, 13 deviations at its foot, `clamped` folded into `panels.py` at the merge; sweeps 135–143 are the owner's): 22 subcommands over a 1986-line cog; a five-state × two-mode table, every button routed to an existing `do_*`; staff bypass the allowed-role gate and may reassign any channel (staff final say, settled in-doc). Fork F1: the in-channel eleven-button control post — leave it (recommended) / rebuild from the same table / drop it. Six findings at its foot (`region_choices` drops `warsaw`; `Said` vs `str` returns; a bare `tempvoice.mode` log). Build est. 420–480k — top of the band |
| [`raidtrain-panel-design.md`](raidtrain-panel-design.md) | **Wave 3 — `/raidtrain` is ONE command** (2026-09-04, ✅ **SHIPPED in v76** `2dd2689` 16:56 — built on `worktree-agent-aba5d44f8e27a8e28` for 473k against a 380–450k estimate, four commits off `4523118`, clean merge, sweeps **163–172**; design Fable-reviewed approve; 595 lines, doc cost 250k): 15 subcommands over two slots become one member-visible panel — `commands synced` **38 → 37**; the mode gate moves onto the panel (it always opens); root states R0–R5, a lineup card whose row 2 stays ≤5, sub-panels, Setup with a Clear… select, `TrainModal` reused. Nine extractions gain `via` (`cancel_train` closes the recorded Via-labelling gap: website cancel logged Via=Discord and `web.raidtrain.cancel` was never written); two disagreeing `DESCRIPTION_LIMIT`s (events 1000 vs raidtrain 500 — 500). Proposes `panels.still_allowed(interaction, ok, refusal)` — the conductor adds it to `panels.py` on `main` before any wave-3 build. Forks F-R1 (lineup-post button), F-R2 (past trains), F-R3 (one claim select vs a next-open-hour button) — all recommend (a). Build est. 380–450k |
| [`role-menus-panel-design.md`](role-menus-panel-design.md) | **Wave 3 — `/rolemenu` + `/role` become ONE command** (2026-09-04, ✅ **SHIPPED in v77** `43312b9` 20:51 — built on `worktree-agent-ace2086f9f7cfa541` for 529k against a 420–500k estimate, four commits off `4523118`, eight append-shaped conflicts, sweeps **173–182**, `commands synced` 37 → 36 measured; design Fable-reviewed approve; 622 lines, doc cost 230k): **18** leaf subcommands (`panels-program.md` §3 said 17) over two staff groups become one staff panel — `commands synced` **38 → 37**; `HIDDEN_WHEN_OFF["rolemenu_mode"]` deleted (the mode ships off, so hiding the command hid the only way on — settled by applications "Visible" + memory I-M1); root, menu card (row 0 exactly five), Add a role / Hand roles out / Grants / grant card / Waiting on staff sub-panels; `End it now` makes the phantom `/role revoke` (named in four docs, never existed) real. Riskiest layer: grant/extend reconciled from TWO divergent implementations (cog inline vs `roles.py`), so `tests/api/tools/test_roles.py` changes; seven bare log kinds gain `kind_via`; ⚠️ `role_menus.py option_label` collides by name with `panels.option_label` — import aliased. Forks F-R1 (pending-requests queue in the panel), F-R2 (grant with no end date, 0 = never), F-R3 (edit re-posts a posted menu) — all recommend (a). Build est. 420–500k |
| [`chat-panel-design.md`](chat-panel-design.md) | **Wave 3 — `/chat` is ONE command** (2026-09-04, ✅ **SHIPPED in v75** `251dd14` 15:58 — built on `worktree-agent-ab33797d235cf0d96` for 441k against a 300–360k estimate, five commits off `5db58fb`, five append-shaped merge conflicts against automod, sweeps written as `C1`–`C8` and numbered **155–162** at the merge; design Fable-reviewed approve; 533 lines, doc cost 241k): nine leaf subcommands under one group become one staff panel — `commands synced` **38 → 38** (a group was already one slot); no shadow mode anywhere in chat (measured); `chat_status_admin_only` now hides the spend BLOCK, not the whole command (forced: the same command carries Knowledge and Personality); Personality (voice select + two mood selects, never one toggle), Knowledge (25-capped select + Find… + note card; `server` notes render neither Remove nor Edit and say why), Settings; new pure `black_bloc/chat_panel.py`; five website `note()` calls deleted under the AST guard `tests/test_logkinds.py`. One key `chat_panel_minutes`. Forks F-C1 (Settings: read-only + one Limits… modal), F-C2 (Edit… a note), F-C3 (confirm on turning the models on — recommends one click), F-C4 (the site's two mood-pool guards onto the Discord door) — recommend (a) each. Build est. 300–360k |
| [`mod-panel-design.md`](mod-panel-design.md) | **Wave 4 — `/mod` is ONE command over the case record** (2026-09-05, ✅ **SHIPPED v81 `a90f416`**, built on `worktree-agent-ab5740f777a3dfd80` off `97531cc`; not run against Discord by eye, sweeps 208–218): `/case`, `/cases` and the `mod` group's `logs` child retire into one staff panel — `commands synced` **36 → 34 on its branch, 35 → 33 on `main` after modmail A, measured**. ⚠️ **The one panel build with a MIGRATION**: schema **28 → 29**, `mod_cases` gains `note`, `note_by`, `note_at`, `voided_at`, `voided_by`, `void_reason`, all nullable, and it was RUN against a database built by the code at `97531cc`. The panel is a root list with a real pager (`‹ Newer` / `Older ›`, the program's first) and a case card carrying the four corrections a case never had — Edit reason…, Add a note… / Edit the note…, Void this case… and Restore this case, never Void and Restore at once. A voided case is marked and struck through, NEVER deleted, and voiding undoes no punishment — the card says so. Four shared functions with `via`, four log kinds, four new API routes and four drawer controls on the Moderation page (`check.mjs` 142 → 146 routes). One key `mod_panel_minutes`. ⚠️ **Fork F1 of `panels-program.md` is ANSWERED, not built**: the seven bare actions stay bare, so the punishment path has zero edits and its 220 lines of tests are unchanged — that is the build's own proof. Owner forks **F-M1 = (a)** voided cases listed in place struck through, **F-M2 = (a)** a voided warn stops counting toward `automod_warn_threshold`, **F-M3 = (a)** the website gets the four moves now. Four defects REPORTED not fixed: bare `mod.purged`/`mod.purge_failed` kinds, the dead `CASE_KINDS` tuple, the site's Notes filter chip that has always matched zero rows, and the rest of the stale `LOG_LEVEL_COMMANDS` map. Est. 340–420k |
| [`automod-panel-design.md`](automod-panel-design.md) | **Wave 3 — `/automod` is ONE command** (2026-09-04, ✅ **SHIPPED in v74** `0b1b2bf` 15:45, synced 38 measured on the boot log; built on `worktree-agent-ae7bb4ba9c5540ad4` for 370k, deviations at its foot, one merge-time relabel; sweeps 144–154 are the owner's; 626 lines, doc cost 209k): eight leaf subcommands under one group become one staff panel — `commands synced` **38 → 38**; three modes (off/shadow/on) and `on` is NOT OFFERED while `staff_channel_id` is still the test channel or resolves no staff role (`arming_refusal` read by both the select and `set_mode`); root = the whole `/automod status` block, rule card (row 0 exactly five for `bad_words`), Exemptions with one removal select carrying role/channel kinds; `_as_words` learns newlines so the Words… modal matches the site's textarea. Enforcement path untouched (the diff must show zero edits inside `punish`/`do_delete`/`do_timeout`/`_answer_for`). Keys `automod_panel_minutes`, `automod_arm_needs_confirm`. Two defects REPORTED not fixed: the generic settings API can arm `automod_mode=on` past both arming refusals; `LOG_LEVEL_COMMANDS["pings"]="pingroles"` names a retired command. Forks F-A1 (confirm before arming — recommends yes, default true), F-A2 (bad_words as one prefilled paragraph field), F-A3 (no controls for `automod_warn_threshold`/`mod_dm_on_action`). Build est. 300–360k |
| [`modmail-panel-design.md`](modmail-panel-design.md) | **Wave 4 — `/modmail` is ONE command, and every ticket carries a sticky staff card** (2026-09-05; ✅ **Build A SHIPPED v80 `b926d9d`**, sweeps 200–207; ✅ **Build B SHIPPED v82 `f42a591`**, sweeps 219–230, forks F-M1–F-M8 all (a); ✅ **follow-up SHIPPED v85** — **A ticket…** and the ticket card ON the panel, sweeps 245–251; **none of the three run against Discord by eye**): Build A = the root panel (`Setup…`, `Blocked…`, `Snippets…`, `Forget…`, `Logs`), the `modmail` and `snippet` groups and eleven leaves retired — `commands synced` **36 → 35**; six pure extractions so the website and Discord write one log row each through one function; `web.modmail.*` kinds renamed to match; `modmail.reply` row new; `modmail_panel_minutes`. Build B = the schema migration (30), the STICKY staff card re-posted on every write into the ticket, `modmail_reply_style` (`buttons` / `typing relays` / `both`), the practice ticket, and `/areply` `/note` `/close` retiring into the card. Forks F-M1–F-M8 in §I. |
| [`settings-panel-design.md`](settings-panel-design.md) | **Wave 4 — `/settings` is ONE command over the cross-cutting panel, the LAST panel of the program** (2026-09-05; ✅ **Build 1 SHIPPED v83 `57a878d`** — the pure `settings_panel.py` module, `namespace_of` into the store, `set_key`/`clear_key`/`reapply_presence`, keys `settings_panel_minutes` + `settings_core_keys_admin_only`; ✅ **Build 2 SHIPPED v84 `ce97de0`** — the panel, `/settings show\|set\|set-role\|set-value\|clear` and the `presence` Group retired, tree **30 → 29 with ZERO `app_commands.Group`s**, sweeps 231–244, forks F-S1–F-S5 all (a); **neither run against Discord by eye**): root modes read-only with `Turn a feature back on…`, `A setting group…` → typed key cards with `Put the default back` (confirm on `staff_channel_id`), `Roles & channels…` behind `manage_guild`, `How Black Bloc looks…` with `Re-apply presence`, `Panels & commands…`, `Log levels…`, `Logs`. Two deviation feet. |
| [`selftest-design.md`](selftest-design.md) | **Wave 5 — the self-test** (owner 2026-09-05: "test it all, can we build api test and endpoints" → "do a but after 5 minutes purge the discord chat of all test, keep the logs on the website tho under test"; ✅ **SHIPPED v86**, sweeps 252–261, schema 30 → 31, **not run by a person**): one `selftest.run()` behind three doors (boot log line, `/settings` ▸ **Run the self-test**, `POST /api/selftest`), four check families (config keys resolve + permissions, every panel's root card POSTED live, every website read, scheduled senders), every message purged after `selftest_purge_minutes` (5), log feature **Test** hidden from the Logs page's default view, Health-page card, `tests/live/` against the deployed api behind `BLACK_BLOC_LIVE_URL`/`BLACK_BLOC_LIVE_TOKEN`. Forks F-ST1–F-ST3. |
| [`honeypot-panel-design.md`](honeypot-panel-design.md) | **Wave 3 — `/honeypot` is ONE command** (2026-09-05, ✅ **SHIPPED v79 `361eaa2`**, merged `fa845a6`; deviations at its foot; **not run against Discord by eye**, sweeps 188–199): the `honeypot` and `exempt` groups and all **seven** leaf subcommands become one staff panel — `commands synced` **36 → 36** (a group was already one slot). Three modes and `on` is NOT OFFERED while no staff role resolves (`arming_refusal` read by both the select and `set_mode`); root = the whole `/honeypot status` block plus a line for a recorded id Discord lost and a line for test mode; ONE `RoleSelect` at `min_values=0` IS the exempt list, with **Exempt nobody** beside it and the picker WITHHELD above 25 stored roles (submitting a capped select would silently delete what it could not show); **Forget…** is a picker, so no channel id is ever typed. New pure `black_bloc/honeypot.py` + `tests/test_honeypot.py` (neither existed). `honeypot.exempt_add`/`exempt_remove` retire into one `honeypot.exempt_set` carrying the added/removed diff; `honeypot.settings` is new; `honeypot.mode` and `honeypot.trap_removed` gain `kind_via`. The catching, banning and Ban-now paths are untouched. One key: `honeypot_panel_minutes`. Two defects REPORTED not fixed: the generic settings API can arm `honeypot_mode=on` past the arming refusal and write `honeypot_exempt_role_ids` with no log row at all; `LOG_LEVEL_COMMANDS` still names ten retired subcommands (the first is KI-21 now). Forks **F-H1** (Exempt nobody beside the select), **F-H2** (Forget as a picker), **F-H3** (purge days on the Numbers… modal) — all (a). |
| [`pings-panel-design.md`](pings-panel-design.md) | **Wave 2 — `/pings` is ONE command** (2026-09-03; body §B–§J RESTORED 2026-09-11 after `8426b1a` cut it, pre-build §A/§E/§G/§H in `../archive/`; ✅ **SHIPPED v72 `a5ad521` 18:30** — merged clean after Fable review, seventeen deviations at its foot; boot measured `commands synced` 40 → 39; **not run against Discord by eye**, sweeps 126–134): both groups retire; Streamers sub-panel with Remove + `Make the role again`; one shared optional-role pick step; Settings sub-panel; `Open on the site` staff-only (every `/api/pings/*` is staff); unfollowing is never mode-gated. One key: `pings_panel_minutes`. Forks I1 (may a streamer remove a ping role staff started) and I2 (one Events toggle or two when the feed keys differ). Seven findings (`site_page_url` ×4, `events_role_id` collapses the two keys, `personas.py:80–82` names commands this deletes). Build est. 300–360k |
| [`youtube-panel-design.md`](youtube-panel-design.md) | **Wave 2 — `/youtube` is ONE command** (2026-09-03, ✅ **SHIPPED v71 `b764757` 17:37** — merged after Fable review, eleven deviations at its foot; boot measured `commands synced` 40; `youtube_mode` shadow since 17:42; **not run against Discord by eye**, sweeps 118–125): both groups and their nine subcommands collapse into one member-visible command — `commands synced` **42 → 41, measured**. The mode select sits on the ROOT staff panel and `Link my channel` still renders with the mode off; keys `youtube_panel_minutes` and `youtube_unlink_dms_them`; the DB layer stayed in the cog. Owner forks **decided 2026-09-03 16:10–16:15: F-Y1 = keep `/youtube` and `/golive` separate; F-Y2 = flip `youtube_mode` to shadow at landing** (operational — done by the conductor on the site 17:42, after the v71 deploy). §K's finding 1 was the one the build had to fix (checklist 10: a link whose feed would not answer no longer claims "0 counted as seen"); the other five are reported and left. Its `## Deviations` foot lists eleven departures, notably `link_channel` raising `LinkRefused` and returning a 3-tuple so the web keeps its 400/409 shapes, and the staff embed keeping the linked list as LINES beside the picker |
| [`memory-panel-design.md`](memory-panel-design.md) | **Wave 2 — `/memory` is ONE command** (2026-09-03, ✅ **SHIPPED v68 `cb941d9` 16:48**): member-only panel, six-row state table, `Forget one of these…` select with a `Forget by words…` modal only above the 25 cap, Yes/Keep confirms; `commands synced` stays **42, measured** (a group was already one slot). One key: `memory_panel_minutes`. Fork **I-M1 decided by the owner 2026-09-03 16:12 — OPEN IT**: `/memory` stays visible while `chat_memory_mode` is off and the panel says memory is off as a LINE, because turning it off does not delete the profiles already stored. Its `## Deviations` foot lists fifteen departures, notably `Forget by words…` rendering for an opted-out member too, `db_up` moving into `panels.py` rather than being copied, and the route's hand-built `web.` kind folding into `forget_profile(via=)`. Findings left unfixed: the Settings page and the mock still say `/settings set chat_memory_staff_view full` (a subcommand that takes a channel), and five wave-1 panels edit without `allowed_mentions` |
| [`phase1-design.md`](phase1-design.md) · [`phase2-design.md`](phase2-design.md) · [`phase3-design.md`](phase3-design.md) · [`phase4-design.md`](phase4-design.md) · [`phase5-design.md`](phase5-design.md) · [`phase6-design.md`](phase6-design.md) · [`phase7-design.md`](phase7-design.md) · [`phase8-design.md`](phase8-design.md) | Per-phase build specs (the builder's brief source), ✅ **ALL EIGHT SHIPPED** (each has its landing entry in [`../DONE.md`](../DONE.md); the dashboard is live at https://blackbloc.heygabi.ai): 1 settings/log/role menus · 2 go-live · 3 temp voice + honeypot · 4 events · 5 birthdays · 6 moderation (shadow) · 7 modmail · **8 the config website**. ⚠️ Their COMMAND surfaces are all superseded by the panels program — read `panels-program.md` and the per-feature `*-panel-design.md` for what a feature's front door is today |
| [`phase19-design.md`](phase19-design.md) | **Applications — the Twitch Team form** (2026-09-02, BUILDABLE, member request #2): staff-defined forms (≤5 questions, Discord modal cap) that grant a role on approve, schema 25 `application_forms`/`application_questions`/`applications`, `/apply` + `/applications`, Approve/Deny cards, DMs, the named "send the twitch.tv invite" nudge (no Twitch Teams API exists), an Applications section on the Role menus page; 7 `applications_*` keys. §J = prove the role add is not read as by-hand by the Phase 9 reconciler. §K = parallel-build rules (merge order 17 → 18 → 19). ✅ **SHIPPED** merge `7b1c592`, deployed `7b1c592` 2026-09-03 (`applications_mode` off) — 9 deviations at the foot of the file; residuals KI-17/KI-18 |
| [`phase18-design.md`](phase18-design.md) | **Raid trains** (2026-09-02, BUILDABLE, member request #1): schema 24 `raid_trains`/`raid_slots`, `/raidtrain` (claim/release/status, organizer create/assign/swap/lock) + `/raidtrains` staff group, lineup post edited in place + thread, 30-min reminder DM, live check-in from go-live sessions, a Raid trains section on the Events page; 13 `raidtrain_*` keys. §K = the parallel-build rules (built beside Phase 17, merge order 17 → 18). ✅ **SHIPPED** merge `0bb3835`, deployed `7b1c592` 2026-09-03 (`raidtrain_mode` off) — §J found Phase 4's calendar helper NOT reusable (it writes to the `events` table), so `raidtrain_scheduled_event` ships **`false`** with a creator of its own; see the `## Deviations` list at the foot of that file and the `# Phase 18` section of `code-notes.md` |
| [`applications-no-role-design.md`](applications-no-role-design.md) | **Applications without a role — "let's have the bot store the info!"** (2026-09-03, ✅ **SHIPPED** — merged `9891f71` 12:40, live in v62 12:48, schema 28; 5 deviations at the foot; sweeps rows 69–72 are the owner's by-eye checks — member question relayed by the owner): a form's role becomes optional (schema 28, `role_id` nullable via a table rebuild that MUST run before `PRAGMA foreign_keys=ON` — §C1 says why), approving a no-role form stores the record and DMs the decision without touching Discord roles, the **roster is the approved applications** (one home, no copy) read per form on the Role menus page with each member's Twitch login from `golive_links` and a Copy-as-text bridge to the official team page; new terminal status `removed` (reason DM'd) is the way off a list; `GET /api/applications/roster`, `POST /{id}/remove`; one setting `applications_roster_shows_left`. §D four owner-flippable calls; §E keeps the temporary-role idea and per-entry Twitch edits out |
| [`requests-check-design.md`](requests-check-design.md) | **Requests, sixth pass — "Ask them to check"** (2026-09-03, **SHIPPED** — merged `44170f4`, live in v61 at 12:29, schema 27; 11 deviations at the doc's foot, the first a review card that would have crashed on render with six buttons in one action row; owner checks are sweeps 66–68), owner ask "ping the requester from the request app … message the requesters to check the work"): a staff ACTION on the review card (panel + site, one shared `ask_check`, one log row `request.check_asked`, routine) that DMs the requester a `check_asked`-look card ("ready for you to try", built + how-to-test) and, when their DMs are closed, pings them in the request channel instead (`request_check_fallback_channel`, default on); schema 27 `check_asked_by`/`check_asked_at` shown as "Asked to check · who · when" on review/done cards; `request_check_on_ready` (default off) asks automatically at ready; `check_asked` joins `request_channel_moves` (off by default); `POST /api/requests/{id}/check`. NOT a state — §B says why; §D lists the six calls the owner may flip; §E keeps DM reply buttons out of scope |
| [`requests-panel-design.md`](requests-panel-design.md) | **Requests, fourth pass** (2026-09-03, ✅ **SHIPPED** — merge `ba5cb99`, deployed the same day; 11 deviations, 9–11 the review findings): `/request` becomes ONE command that opens an ephemeral panel — member panel (File, withdraw-with-confirm; the own list is staff-only, `request_panel_own_list` off by default), staff panel (counts, a 25-cap select of open requests, Logs), the request card with ONLY the moves valid from its state as buttons (table per status), modals for ready / send back / hold / decline, every move through the existing shared functions; `request_panel_minutes` and `request_panel_own_list` settings; the nine subcommands go. **The PATTERN for "minimise slash commands, maximise interactive windows"** (owner rule, `CLAUDE.md`) — the rest of the app follows feature by feature |
| [`requests-embeds-design.md`](requests-embeds-design.md) | **Requests, third pass** (2026-09-03, ✅ **SHIPPED** — merge `355d6e9` + anchor fix `70a6720`, deployed `70a6720` 2026-09-03 06:34, landing one-off run 06:41): the state machine grows a **`review`** ("ready to check") state — `in_progress → review → done`, `done` reachable from nowhere else, `built` required to enter it, Accept / Send back — and every request notification becomes ONE embed builder used by channel and DM (seven looks: filed / in_progress / review / sent_back / done / hold / declined) with a link button to `{origin}/requests.html#r-{id}`; schema 26 `built` / `how_to_test` / `ready_by` / `sent_back_reason`; `request_channel_moves` (a new `enums` registry type) + `request_review_by_other` settings; a Ready-to-check section and per-card anchors on the Requests page. Its `## Deviations` foot lists 14 departures (12 = the labels.js blank-dashboard defect, 14 = the link is `requests.html`), what §J measured, and the landing one-off that moved requests #1 and #2 `done → review` (RUN 2026-09-03 06:41; #3 stayed `hold`) |
| [`requests-states-design.md`](requests-states-design.md) | **Requests, second pass** (2026-09-02, ✅ SHIPPED with Phase 17 — merge `6d61994`, deployed `7b1c592` 2026-09-03): the owner's state machine `open → in_progress → done` with `hold` (reason required, remembers `held_from`) and `declined` as side states; `pending`/`approved`/`planned` and `request_auto_approve_staff` retired; requester DM + channel line on EVERY staff move via the new `request_status_channel_id`. Supersedes the status part of phase13-design. Its `## Deviations` foot lists where the build departed from it |
| [`raid-train-capture.md`](raid-train-capture.md) | **Raid trains — r3dlabs.com feature inventory** (2026-09-02, captured for member request #1): every feature of the site bucketed ASKED / FIT / LATER / NO against what Black Bloc already has, plus the minimal build the request actually needs (event + slots, claims, availability, 30-min reminder DM) — none of which needs a Twitch OAuth grant |
| [`phase17-design.md`](phase17-design.md) | **Chat long-term memory** (2026-09-02, ✅ **SHIPPED** merge `6d61994`, deployed `7b1c592` 2026-09-03, `chat_memory_mode` off — D1–D5 all decided by the owner 17:20–18:38, one at a time): per-person preference profiles distilled from the expiring `chat_window` on the hourly sweep (Groq, never on the reply path), schema 23 `chat_profiles`/`chat_memory_optout`, `/memory show\|forget\|forget-this\|off\|on`, a Memory section on the Chat page. **§J is measured** — 29/29 distillations parseable on `openai/gpt-oss-120b`, median 0.77 s, and what the measurement CHANGED is written into §J and the `# Phase 17` section of `code-notes.md`. Its `## Deviations` foot lists where the build departed from it (`/memory` not `/chat memory`; no `chat_memory_log_level`). The Phase 17 build also carries `requests-states-design.md` |
| [`phase16-design.md`](phase16-design.md) | **YouTube uploads (F3)** (2026-09-02): public Atom feed primary (no key), optional `YOUTUBE_API_KEY` for handle resolution + live/Shorts classification, schema 22 `youtube_links`/`youtube_videos`, ⚠️ **its command surface superseded 2026-09-03 by [`youtube-panel-design.md`](youtube-panel-design.md)** (the `/youtube` and `/uploads` groups are retired; everything else in it still stands), seed-never-backfill rule, section on the Go-live page; ten defaults-as-settings decisions. ✅ **SHIPPED** merge `7295f61`, live 2026-09-02 (`youtube_mode` off) — see the `# Phase 16 (F3)` section at the foot of `code-notes.md` for what §J's measurements changed (no ETag ever sent; Shorts are free from the `/shorts/` link; the feed answers ~50% of the time — KI-11/12/13) |
| [`phase15-design.md`](phase15-design.md) | **Ping roles (F14)** (2026-09-02, ✅ **SHIPPED** `d777f57` — landing entry in [`../DONE.md`](../DONE.md), *"2026-09-02 — Phase 15: ping roles (F14)"*; `pings_mode` was flipped **on** from the dashboard the same evening. ⚠️ `/pingroles` and `/pings follow` are RETIRED — the surface is one `/pings` panel, [`pings-panel-design.md`](pings-panel-design.md), v72): the opt-in Events role made self-serve and per-streamer fan roles (schema 21 `golive_fan_roles`, auto-paged Streamers panels, dashboard Pings section); the seven defaults-as-settings decisions |
| [`personality-pool-design.md`](personality-pool-design.md) | **Next-wave #4 — the global personality pool** (2026-09-05, ✅ **BOTH HALVES LIVE** — Black Bloc **v90** `7c59eb1` + **v91** `604226f`, GABI `de4ef63` deployed `755cfd54`; the self-test's `pool.in_step_with_gabi` check took the boot line 106 → **107 ok, 0 failed** and it has read 107/0 at every deploy since; **KI-23 closed**. This row said "DESIGN, not built" until 2026-09-11): share the SKELETON (roster, graph, drift constants, clause templates in one canonical `personality-pool.json` in `catalog-platform`), keep the SKIN (each bot's own voice bodies); Black Bloc boot sync that never touches staff's `enabled`; retired-not-deleted tropes; `/health` version field + one self-test read check against GABI's health route so drift is VISIBLE. A live shared store is rejected (§2: owner-locked roster vs staff-editable rows, voices meant to differ, no fetch on a boot path — follows GABI's §11.4 precedent). Forks F-P1–F-P3, recommend (a) each. Est. Black Bloc half 150–220k, GABI half 80–120k |
| [`phase14-design.md`](phase14-design.md) | **Chat step 3** (2026-09-01, ✅ **SHIPPED, dormant** — landing entry in [`../DONE.md`](../DONE.md), *"2026-09-01 — Phase 14: the bot can really talk"*; the modules it created are `chat_llm.py`, `llm.py`, `groq.py`, `knowledge.py`, `personas.py`, and its command surface is superseded by [`chat-panel-design.md`](chat-panel-design.md), v75): three-tier @-mention replies (intents free → Groq/Llama simple → Haiku important), GABI-style knowledge ingestion + lexical search, Cookout persona + the ported trope pool, $20/mo cap with ledger + fuses; two slices 14a/14b |
| [`cutover-plan.md`](cutover-plan.md) | **How the bot goes from test mode to running the server** (2026-09-01, ⏸️ **NOT STARTED** as of 2026-09-11 — `TEST_MODE` is still ON and not one row of its §2 ladder has been executed): prerequisites, the per-feature shadow→watch→on ladder in risk order, incumbent retirement, standing risks. The owner paces it |
| [`site-restyle-design.md`](site-restyle-design.md) | **The A/C-hybrid restyle brief** (2026-08-31, ✅ **BOTH SLICES LIVE** — R1 `0c49257` 12:20, R2 `c334922` 13:14, same day): the owner's four decisions, what comes from A vs C, the new default "Black Bloc" theme (dark+light), nav icons. The "build after B4–B8" line was the pre-build plan and is history |
| [`phase13-design.md`](phase13-design.md) | **Requests (F18)** (2026-08-27): `/request` (what / why / due date, requester automatic), staff auto-approved, the pending-features list on a Requests page. ✅ **SHIPPED** `8036918`, live 2026-08-27 20:15 |
| [`phase12-design.md`](phase12-design.md) | **Logs** (2026-08-27): Discord gets only important lines (acted on a member / failed; approvals always notify), per-feature `_log_level`, Logs sections on every page + a global Logs page, `/… logs` on every group. ✅ **SHIPPED** `417ef23`, live 2026-08-27 18:38 |
| [`phase11-design.md`](phase11-design.md) | **Chat 2 (F10 step 2)** (2026-08-27): editable intents/lines page, data intents (live / next / birthdays / count / roles / timezone), modmail routing, manners settings. ✅ **SHIPPED** `5f5741a`, live 2026-08-27 17:05 |
| [`phase10-design.md`](phase10-design.md) | **Polls (F15)** (2026-08-27): the 15 owner decisions, deltas from the research design, the test-mode rule, two build slices. ✅ **SHIPPED** `3eb7e4f` + `ebf99a2`, live 2026-08-27 14:46 / 15:44 |
| [`phase9-design.md`](phase9-design.md) | **Role menus 2** (2026-08-27, ✅ **SHIPPED** — landing entry in [`../DONE.md`](../DONE.md), *"2026-08-27 — Phase 9: approval-gated role menus, timed roles, reconciliation (bot + dashboard)"*; the command surface is superseded by [`role-menus-panel-design.md`](role-menus-panel-design.md), v77): approval-gated menus (staff Approve/Deny, settable channel), time-limited grants with an expiry loop, reconciliation of hand-made role changes — the four owner decisions and the storage/flows/surfaces |
| [`phase8b-design.md`](phase8b-design.md) | The full dashboard: the 13 tabs, the API/page contract both 8b builders code against. ✅ **SHIPPED** (landing entry in [`../DONE.md`](../DONE.md), *"2026-08-27 — Phase 8b merged and reconciled: the API and the pages meet"*, plus its security review's nine findings). ⚠️ **13 tabs is the design's figure; the site has 17 pages / 150 routes** as of v108 — `architecture.md` owns the current count |
| [`polls-research.md`](polls-research.md) | **F15 polls** (this row said "F17" until 2026-08-31 — wrong; F17 is the role-process takeover, and `feature-list.md:53` / `phase10-design.md` both call polls F15): Polly is Slack-only; EasyPoll/Simple Poll vs Discord native polls feature matrix, typed-answer matrix, discord.py 2.7.1 limits measured, the recommended native-plus-wrapper design, 15 owner decisions |
| [`site-feature-audit.md`](site-feature-audit.md) | **Every dashboard section classed control / data / display** (2026-08-27 read-only audit): the ranked A/B/C change list that feeds the "no displays, only controls" build, incl. the Automod `[object Object]` bug |
| [`dashboard-inspiration.md`](dashboard-inspiration.md) | **What the site should LOOK like.** Survey of 11 bot/admin dashboards (Carl, YAGPDB, MEE6, Dyno, Wick, ProBot, Sapphire, Discord, Linear, Vercel, Cloudflare), what is wrong with ours today, and three candidate directions with palettes, type stacks and wireframes |
| [`test-suite-profile.md`](test-suite-profile.md) | ⚠️ **Where the tests actually spend their time** (2026-09-05, MEASUREMENT PASS — report only, nothing changed). ⚠️ **Its figures are a v93 snapshot of a 5188-test suite; the suite is 5546 tests over 106 files as of v108** (re-counted 2026-09-11) — read every ratio and second in it as a 2026-09-05 reading: the owner's "do we truly need 5186 tests?" answered with numbers. **84 % of the suite is fixture `setup`, 13 % is the assertions.** The single heaviest file is `tests/api/test_contract.py` — 3 % of the tests, **14.7 s of the 79.1 s wall clock, measured** (`-n auto` runs in 64.40 s without it), because its 321-line `seeded` fixture is rebuilt for each of 149 routes while its assertions cost 0.78 s. `Database.connect()` (schema v32, on disk) costs **51 ms** and is function-scoped in all 46 files that use it — **168 s of CPU** building 3298 identical databases; `create_app()`+`TestClient` costs **84 ms** × 1035. Against that: **0** skipped/xfail, **0** `assert True`, **0** verbatim duplicates (the 23 the hash found are parametrised over different route tables), and of **36** mirrored file pairs exactly **ONE** true duplicate worth **62 ms**. The two biggest parametrised tables (187 + 160 ids) cost **0.34 s between them**. Verdict: the suite's **size is not the problem, its fixture shape is** — 7 yes/no decisions at the foot, each with the seconds and the coverage it trades |
| [`review-checklist.md`](review-checklist.md) | ⚠️ **Read before building or reviewing any phase** — **35 items**, item 35 (a reversal edits the DECIDED bullet) added 2026-09-11 11:00; 34 re-counted 2026-09-11 and every item's named trace re-checked against `main` (33 counted 2026-08-31; item 34 — one web write, one log row — added 2026-09-03; it began at 20), each traced to a confirmed finding or incident in this repo. ⚠️ **`CLAUDE.md` says "33 items" and is one behind — this file owns the count** |
| [`feature-list.md`](feature-list.md) | **The full feature list** — rows **F1–F20 plus C1**, contiguous as of 2026-09-11: the **F17** row this line used to ask the next toucher to add now exists, and the F18/F19/F20 rows that had been appended BELOW the build-order table (where they rendered as garbage) are back in the feature table. Re-verified against the repo 2026-09-11 — six rows that said a thing was "not merged" or "not deployed" had been live for a week |
| [`architecture.md`](architecture.md) | Package layout, the cog convention, config, storage, the optional API. ⚠️ **It OWNS the fact table** — cogs, commands, schema, registry keys, routes, deploys — re-measured at **v108** on 2026-09-11 (19 / 29 / 34 / 202 / 150 / 107); no other doc should restate those numbers, only link here |
| [`code-notes.md`](code-notes.md) | ⚠️ **The comments the source no longer carries.** Every explanation stripped out of `black_bloc/**` and `tests/**`, keyed by `path:line` (owner rule, 2026-08-26) |
| [`hosting.md`](hosting.md) | Why not Cloudflare Workers; why Fly.io; what the alternatives cost |
| [`gotchas.md`](gotchas.md) | Traps that will cost real time: intents, command sync, OneDrive + SQLite (KI-1, now CLOSED), Ctrl+C with the API on, Defender's ClickFix signature, double-encoded docs, and — added 2026-09-11 — **the `deploy.ps1` xdist hang** (five recorded incidents; the fix is a retry through the PowerShell tool with output to a file) |
| [`reference-bots.md`](reference-bots.md) | Feature-by-feature inventory of the bots we are cloning/replacing: TempVoice, Honeypot, YAGPDB, Carl-bot, Birthday Bot, Modmail — plus Discord platform limits (modals, scheduled events, `<t:…>`, intents) |
| [`mock-direction-a/`](mock-direction-a/) | The **Direction A** design-canvas artboards (`Main`, `AModeration`, `ASettings` as `.dc.html`) the dashboard restyle was drawn from. Not Markdown — open them in a browser or the canvas editor |

Design docs for individual features go here too, one file each
(`<feature>-design.md`), written BEFORE the cog when the feature has a
decision in it.
